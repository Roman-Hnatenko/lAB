#include <stdio.h>
#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
  int n, m;
  cin>>n>>m;
  vector<int> a(n);
  for(int i = 0; i < n; i++)
  {
      cin>> a[i];

  }
  for(int i = 0; i < m; i++)
  {
      int value;
      cin>>value;
      int l =0, r = n - 1;
      int bestIndex = n - 1;
      while(l<=r){
          int  m = (l + r);
      }
      
  }
  
  
};